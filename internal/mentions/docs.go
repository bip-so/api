package mentions

// We can create mentions on almost anything.

// We have a mention's table so I can query by studio/creator/mentioneduser/
// There are 2 saves here
// Save on the object
// User can mention @(user:Handle/branch:Name/role/Name) `{{user|ui}}`

// Block.Mentions [{user},{user}, {user}, {canvas},{role}] /  {{user|id}} {{canavs|id}} {{role|id}}
// BlockThread.Mention
// BlockThreadComment
// Reel
// ReelComment
// Mentions: All these Models have this fields.
// As of now the MENTIONS are EMBDEED IN THE OBJECT
